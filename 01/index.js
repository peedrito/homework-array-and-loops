// Exercício (01) - Some o array;

// Array sugerido do exercício;
const numbers = [2, 3, 4, 6];
let sum = 0;

// Repetição para percorrer o array;
for (let i = 0; i < numbers.length;i++) {
    sum += numbers[i];
}

// Imprime no console;
console.log(sum);